<a href="${gf.filterUrl(content.link)}">
      <span class="ttl">${content.newstitle}</span>
      <p>${content.newssummary}
      <span class="by">${(content.postdate?date)?string.long} - by Admin</span>
      <!-- posted by need to implement -->
</a>
