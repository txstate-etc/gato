jQuery(function() {
	var $ = jQuery;


	$('.grid-container').each(function(i,grid){
		if ( $(grid).find('ul.grid-filter').length ) {
			console.log($(grid).find('ul.grid-filter>li>a'));






		// console.log(grid);
		}
	});
});
